import { StyleSheet } from 'react-native';

const UserShowSceneStyles = StyleSheet.create({
  backdrop: {
    width: '100%',
    height: '100%',
  },
  linearGradient: {
    height: "100%",
  },
  bottom: {
    margin: 100,
  }
});

export default UserShowSceneStyles;
